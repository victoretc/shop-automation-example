# shop-automation-example

# Запуск

1. Установить poetry https://python-poetry.org/docs/
2. poetry install
3. poetry run playwright install chromium --with-deps
4. poetry run pre-commit install
5. poetry run pytest
6. Установить Allure https://allurereport.org/docs/v2/install/
7. allure serve
